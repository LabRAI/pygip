benchmark:
  name: tc
  hazard_task: tc.track_intensity
  metrics:
    - track_error
    - intensity_mae
  eval_split: test
dataset:
  name: tcbench_alpha
  params:
    micro: true
model:
  name: tcif_fusion
  task: regression
  params:
    input_dim: 8
    hidden_dim: 64
    horizon: 5
    output_dim: 3
report:
  output_dir: reports/tcif_fusion
  formats:
    - json
seed: 7
