benchmark:
  name: earthquake
  hazard_task: earthquake.picking
  metrics:
    - p_pick_mae
    - s_pick_mae
    - f1
  eval_split: test
dataset:
  name: pick_benchmark_waveforms
  params:
    micro: true
model:
  name: gpd
  task: regression
  params:
    in_channels: 3
    hidden_dim: 32
report:
  output_dir: reports/earthquake_gpd
  formats:
    - json
seed: 7
